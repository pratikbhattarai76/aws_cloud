services:
  malaideu:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: malaideu
    restart: unless-stopped
    env_file:
      - ../app/src/.env
    ports:
      - "3000:3000"